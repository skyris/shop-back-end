const { CORS_HEADERS } = require('./headers');
const ERROR_MESSAGES = require('./errorMessages');
const STATUS_CODES = require('./statusCodes');


module.exports = {
  CORS_HEADERS,
  ERROR_MESSAGES,
  STATUS_CODES,
};
